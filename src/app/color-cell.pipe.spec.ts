import { ColorCellPipe } from './color-cell.pipe';

describe('ColorCellPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorCellPipe();
    expect(pipe).toBeTruthy();
  });
});
